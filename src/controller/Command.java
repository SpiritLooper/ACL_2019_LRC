package controller;

/**
 * enum representing the different possible actions for the hero to input
 */
public enum Command {

    UP,
    DOWN,
    LEFT,
    RIGHT,
    IDLE,
    ESCAPE,
    SPACE;

}
