package main.java.controller;

/**
 * Different possible actions for the hero and monsters to move
 */
public enum Command {

    UP,
    DOWN,
    LEFT,
    RIGHT,
    IDLE,
    ESCAPE,
    SPACE;

}
